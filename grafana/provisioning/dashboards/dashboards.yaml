apiVersion: 1

providers:
  - name: ride-demand-observability
    orgId: 1
    folder: Ride Demand Observability
    folderUid: ride-demand-observability
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 15
    options:
      path: /var/lib/grafana/dashboards
