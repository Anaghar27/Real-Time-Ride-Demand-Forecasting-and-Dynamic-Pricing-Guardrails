# This file marks the API test package so helper imports stay stable.
# It exists to keep API tests organized under a dedicated namespace.
# The package groups endpoint, schema, and contract checks for Phase 7.
# Explicit package structure helps test discovery remain predictable.
